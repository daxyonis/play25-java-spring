A sample Play 2.5 application that demonstrates how to use a Spring container within the Play application.

Play objects are injected with Guice while application-specific beans are managed inside a Spring container.

Check app/modules/Global.java to see how the Spring container is setup.